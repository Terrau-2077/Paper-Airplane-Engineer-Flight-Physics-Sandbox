# 纸飞机工程师 — 开发需求文档 & 规则手册

## 0. 项目元信息

| 项 | 值 |
|---|---|
| 赛题 | 纸飞机工程师：从折纸到超临界翼型 |
| 时间预算 | 2 小时 |
| 核心目标 | 可交互的飞行物理沙盒 Demo，评委能玩、能看懂航空原理 |
| 评分维度 | 创新性 + 技术实现 |
| 项目路径 | `C:\Users\Yulon\paper-plane-sandbox\` |

---

## 1. 产品定义

### 1.1 一句话描述
一个浏览器里的 3D 纸飞机设计沙盒：调参数 → 折叠纸飞机 → 扔出去 → 看它怎么飞。

### 1.2 用户故事
1. 用户看到一架默认纸飞机悬浮在屏幕中央
2. 拖动滑块调整参数（机翼面积、重心、后掠角、上反角）
3. 纸飞机实时变形，反映参数变化
4. 点击"发射"，纸飞机以一定初速度飞出
5. 观察飞行轨迹、实时受力箭头（升力/阻力/重力）
6. 看到飞行数据：飞行距离、滞空时间
7. 可以重置、重新调整、再次发射

### 1.3 MVP 范围（2小时必须完成）

**必须做（P0）：**
- [x] 3D 纸飞机模型（可变形，反映参数）
- [x] 参数面板：4 个滑块（机翼面积、重心位置、后掠角、上反角）
- [x] 物理解算：升力、阻力、重力、俯仰力矩
- [x] 发射机制：设定初速度和角度，掷出
- [x] 飞行数据显示：实时高度/速度/距离，结束时显示成绩
- [x] 力矢量可视化（箭头显示升力/阻力/重力方向）
- [x] 任务：飞最远（距离赛）

**应该做（P1，如果时间允许）：**
- [ ] 气流粒子可视化（轨迹线）
- [ ] 任务：滞空时间赛
- [ ] 地面目标（飞向靶心）

**不做（P2，明确砍掉）：**
- [ ] 多人在线
- [ ] 存储/回放
- [ ] 鸡蛋投送任务（超出2小时范围）
- [ ] 声音
- [ ] 移动端适配

### 1.4 创新点（评分关键）

| 创新点 | 说明 | 技术实现 |
|---|---|---|
| **实时物理可视化** | 飞行中显示力矢量箭头，用户直观理解升力/阻力/重力关系 | Three.js Arrow + 颜色编码（红=阻力，绿=升力，蓝=重力） |
| **参数驱动的纸飞机变形** | 调滑块时飞机实时折叠变形，不是预置几个模型换来换去 | BufferGeometry 顶点实时计算 |
| **简化但正确的气动模型** | 基于升力公式 L=½ρv²SCL，不是随机动画 | 每帧计算气动力，施加到刚体 |
| **教育性数据反馈** | HUD 显示攻角、升阻比、速度矢量，评委一看就懂航空原理 | Canvas 2D overlay 画 HUD |

---

## 2. 技术方案

### 2.1 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 渲染 | Three.js (CDN 引入，不构建) | 3D 最成熟的浏览器库，评委打开 HTML 就能跑 |
| 物理 | 自研简化气动模型 | 这是创新点核心。不用 Cannon-es 等通用物理引擎，因为通用引擎没有气动力。自己算升力/阻力/力矩，每帧施加 |
| UI | 原生 DOM + CSS | 参数面板就是几个 slider + button，不需要框架 |
| 开发 | 单文件 + ES modules | 一个 `index.html` + 几个 `.js` 文件，零构建 |
| 部署 | 本地打开 `index.html` 即可 | 黑客松评审不需要部署 |

### 2.2 为什么不用 Cannon-es/Ammo.js？

通用物理引擎处理的是**碰撞 + 刚体动力学**，不做**空气动力学**。纸飞机的核心是气动力（升力/阻力），这些力必须自己算。与其在通用引擎上 hack 气动力，不如直接自己写——代码更少、更可控、评委能看懂。

### 2.3 气动模型设计（核心）

```
每帧计算 4 个力：
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│  升力 L   │  │  阻力 D   │  │  重力 W   │  │ 俯仰力矩 M │
│  ⊥ 来流   │  │  ∥ 来流   │  │  ↓ 向下   │  │  绕重心旋转  │
└──────────┘  └──────────┘  └──────────┘  └──────────┘

升力公式：  L = 0.5 × ρ × v² × S × CL(α)
阻力公式：  D = 0.5 × ρ × v² × S × CD(α)
重力公式：  W = m × g
力矩公式：  M = 0.5 × ρ × v² × S × c × CM(α)

其中：
  ρ = 空气密度（常量 1.225）
  v = 当前速度标量
  S = 机翼面积（用户可调）
  α = 攻角（速度矢量与机翼弦线夹角）
  c = 平均气动弦长（与机翼几何相关）
  CL, CD, CM = 气动系数（简化后的 α 函数）

简化假设（标注给评委看）：
  - CL(α) ≈ 2π × sin(α)  (薄翼理论线性段，失速前)
  - 失速角 ≈ 15°，超过后 CL 骤降
  - CD(α) = CD0 + k × CL²  (诱导阻力 + 寄生阻力)
  - 忽略侧滑、滚转（简化为 2D 纵向运动 + 可选 3D 展示）
```

### 2.4 参数映射

| 用户参数 | 物理变量 | 范围 | 默认值 | 对飞机形状的影响 |
|---|---|---|---|---|
| 机翼面积 | S (m²) | 0.01–0.06 | 0.03 | 翼展、弦长同比例缩放 |
| 重心位置 | CG (% 弦长) | 15%–45% | 25% | 重心标记前后移动 |
| 后掠角 | Λ (deg) | 0°–45° | 10° | 机翼向后倾斜 |
| 上反角 | Γ (deg) | 0°–20° | 5° | 翼尖向上翘 |

### 2.5 文件结构

```
paper-plane-sandbox/
├── index.html              # 入口：挂载 Three.js canvas + UI
├── CLAUDE.md               # 本文件（规则 + 需求）
├── src/
│   ├── main.js             # 初始化，主循环
│   ├── core/
│   │   ├── Aerodynamics.js # 气动力计算（升力/阻力/力矩公式）
│   │   └── FlightState.js  # 飞行状态机（待发射→飞行中→已落地）
│   ├── plane/
│   │   ├── PaperPlane.js   # 纸飞机 3D 模型（Three.js Mesh 构建 + 变形）
│   │   └── PlanePresets.js # 预设参数（默认/远距离/长滞空）
│   ├── simulation/
│   │   ├── Launcher.js     # 发射逻辑（初速度矢量 → 启动模拟）
│   │   └── Mission.js      # 任务评判（飞最远/滞空最久）
│   ├── ui/
│   │   ├── ControlPanel.js # 参数面板（4 个 slider + 预设按钮）
│   │   ├── HUD.js          # 飞行中数据显示（高度/速度/攻角/升阻比）
│   │   └── ResultOverlay.js# 结果面板（距离/时间/成绩）
│   └── visualization/
│       └── ForceArrows.js  # 力矢量 3D 箭头（升力绿/阻力红/重力蓝）
└── public/
    └── assets/
        └── (空，需要时加纹理)
```

---

## 3. 开发规则

### 3.1 铁律

1. **先 demo 再 polish**：功能跑通 > 代码优雅 > UI 好看。2 小时内只关注"能跑"。
2. **每个模块写完立刻验证**：写完一个 .js 文件 → 刷新浏览器 → 确认 console 无报错 → 继续下一个。
3. **物理公式绝不修修补补**：升力/阻力公式严格按文档实现，不做"调参让它看起来对"的黑魔法。公式正确性本身就是评分点。
4. **所有参数和常量集中管理**：物理常量、默认参数、范围约束集中在各自模块顶部，不散落各处。
5. **HTML 文件用 CDN 引入 Three.js，不做构建**：npm install → 等 → 配 webpack → 调半天，2 小时已经没了。
6. **模块间依赖单向**：`main.js` → 各模块 → 无反向依赖。`core/` 不依赖 `ui/`，`plane/` 不依赖 `simulation/`。

### 3.2 编码规范

- 文件名：PascalCase（类模块）、camelCase（保留给未来工具函数）
- 每个模块 export 一个 class 或一个 plain object，不暴露内部变量
- 物理计算用 SI 单位制（米、秒、千克），显示时转换
- console.log 只在开发阶段用，每个关键步骤打一条，评审时保留（展示思路）
- 注释用中文，代码用英文

### 3.3 验证清单（每步必须执行）

写完每个模块后：
1. 浏览器打开 `index.html` 
2. F12 打开 console
3. 确认无红色报错
4. 确认该模块的 console.log 输出符合预期
5. 功能交互是否正常

### 3.4 评审视角

评审时评委可能问的问题，代码/注释里提前回答：
- "为什么自己写物理而不是用引擎？" → 注释说明
- "升力公式怎么来的？" → 代码注释里写公式
- "失速怎么模拟？" → `Aerodynamics.js` 里实现失速行为
- "纸飞机怎么变形的？" → `PaperPlane.js` 里顶点操作逻辑

---

## 4. 实现计划（按时间分阶段）

### Phase 1：骨架（0:00–0:25）
- [ ] 创建 `index.html`（CDN 引入 Three.js + 挂载 canvas + UI 骨架）
- [ ] 创建 `src/main.js`（初始化 Three.js 场景、相机、光照、地面、动画循环）
- [ ] 创建 `src/plane/PaperPlane.js`（构建一个静态纸飞机 Mesh）
- [ ] **验证点**：浏览器看到一架纸飞机 + 地面

### Phase 2：物理核心（0:25–0:50）
- [ ] 创建 `src/core/Aerodynamics.js`（实现升力/阻力/力矩公式）
- [ ] 创建 `src/core/FlightState.js`（状态机：idle → flying → landed）
- [ ] 在 `main.js` 循环中调用气动计算，更新飞机位置/姿态
- [ ] **验证点**：console 输出每帧的升力/阻力/位置，数值合理

### Phase 3：交互（0:50–1:20）
- [ ] 创建 `src/ui/ControlPanel.js`（4 个 slider + 发射按钮）
- [ ] 创建 `src/simulation/Launcher.js`（发射初速度 + 角度）
- [ ] 创建 `src/simulation/Mission.js`（距离计算 + 结果判定）
- [ ] 纸飞机参数变化 → 实时变形
- [ ] **验证点**：调滑块 → 飞机变形 → 点发射 → 飞出去 → 显示距离

### Phase 4：可视化 & 打磨（1:20–2:00）
- [ ] 创建 `src/visualization/ForceArrows.js`（力矢量箭头）
- [ ] 创建 `src/ui/HUD.js`（飞行数据面板）
- [ ] 创建 `src/ui/ResultOverlay.js`（结果面板）
- [ ] 创建 `src/plane/PlanePresets.js`（预设按钮）
- [ ] 整体打磨：相机跟随、颜色、文案
- [ ] **验证点**：完整体验流程，无报错，数据合理

---

## 5. 物理常量 & 默认参数

```js
// 环境常量
const RHO = 1.225;        // 空气密度 kg/m³
const G = 9.81;           // 重力加速度 m/s²

// 纸飞机默认参数
const DEFAULT_PARAMS = {
  wingArea: 0.03,         // 机翼面积 m²
  cgPosition: 0.25,       // 重心位置 (% 弦长，从前缘起)
  sweepAngle: 10,         // 后掠角 deg
  dihedralAngle: 5,       // 上反角 deg
};

// 发射默认参数
const DEFAULT_LAUNCH = {
  speed: 15,              // 初速度 m/s
  angle: 10,              // 发射仰角 deg
  height: 1.5,            // 发射高度 m
};

// 物理边界
const STALL_ANGLE = 15;   // 失速攻角 deg
const GROUND_Y = 0;       // 地面高度 m
```

---

## 6. 风险点 & 应对

| 风险 | 概率 | 应对 |
|---|---|---|
| 气动模型数值发散（飞机越飞越快/原地转圈） | 中 | 加数值阻尼、限制最大速度、时间步长截断 |
| Three.js CDN 加载慢/被墙 | 低 | 备一个本地文件方案 |
| 纸飞机变形后看起来不像纸飞机 | 中 | Phase 1 先做简化版三角翼，后面再加折叠细节 |
| 2 小时不够写完所有 P0 | 高 | 严格按 Phase 顺序，P0 完成就停，不提前做可视化 |

---

## 7. 发给评委的说明（一句话）

> 这是一个基于薄翼理论的实时飞行物理沙盒——不需要发动机和燃料系统，只靠机翼面积、重心、后掠角、上反角四个参数，就能直观理解波音 787 的空气动力学原理。
